name: Build on Push

on: [push]

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macOS-latest]
        include:
          - os: ubuntu-latest
            output-name: kyure-linux
          - os: macOS-latest
            output-name: kyure-mac
          - os: windows-latest
            output-name: kyure-windows.exe

    steps:
      - uses: actions/checkout@v1
      - uses: palmerov/gh-actions/setup-dart@master
      - run: mkdir build
      - run: dart2native bin/main.dart -v -o build/${{ matrix.output-name }}
      - uses: actions/upload-artifact@v1
        with:
          name: native-executables
          path: build
